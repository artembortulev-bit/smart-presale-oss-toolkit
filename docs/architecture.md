# Architecture Notes

The public architecture document is [ARCHITECTURE.md](ARCHITECTURE.md).

This legacy filename is kept so older links do not break. Private source paths and internal import notes were removed for OSS readiness.
